__pycache__/
*.py[cod]
.pytest_cache/
server/.pytest_cache/
server/tests/.tmp*/
server/data/
data/
web/node_modules/
web/dist/
web/.pytest_cache/
web/tsconfig.tsbuildinfo
.env
*.log
test_bill.png
Ledger-*.zip

# Session-signing key, generated on first run. Publishing it would let
# anyone forge a login for this ledger.
secret.key

# Real business data used while developing — never publish
References/

# Throwaway databases the test suite writes, one per parallel worker.
server/tests/.tmpdata*/
